# Nginx配置示例
# 将此配置放在 /etc/nginx/sites-available/html-uploader
# 并创建软链接到 /etc/nginx/sites-enabled/

server {
    listen 80;
    server_name your-domain.com;  # 修改为你的域名或IP

    # 上传工具访问路径
    location /upload/ {
        proxy_pass http://127.0.0.1:5000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 上传文件大小限制
        client_max_body_size 100M;
        
        # 超时设置
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }
    
    # 静态文件访问
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        autoindex on;  # 开启目录浏览
        autoindex_exact_size off;  # 显示文件大小单位
        autoindex_localtime on;  # 显示本地时间
    }
    
    # 安全设置
    location ~ /\.git {
        deny all;
    }
    
    # 日志
    access_log /var/log/nginx/html-uploader-access.log;
    error_log /var/log/nginx/html-uploader-error.log;
}

# HTTPS配置（可选，推荐）
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#
#     ssl_certificate /path/to/cert.pem;
#     ssl_certificate_key /path/to/key.pem;
#
#     # 其他配置同上
#     location /upload/ {
#         proxy_pass http://127.0.0.1:5000/;
#         # ... 其他proxy设置
#     }
#
#     location / {
#         root /usr/share/nginx/html;
#         # ... 其他设置
#     }
# }
#
# # HTTP重定向到HTTPS
# server {
#     listen 80;
#     server_name your-domain.com;
#     return 301 https://$server_name$request_uri;
# }
